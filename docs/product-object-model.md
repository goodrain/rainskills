# Rainbond Product Object Model

The installable canonical model lives at
[rainbond-app-assistant/references/product-object-model.md](../rainbond-app-assistant/references/product-object-model.md).

This repository document is intentionally an index rather than a second copy. Keeping one
installable source of truth prevents the npm package and repository documentation from
drifting. Update the canonical reference first whenever a shared object, enum, cross-skill
boundary, or structured-output contract changes.
